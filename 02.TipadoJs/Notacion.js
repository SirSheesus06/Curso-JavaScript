// Notacion

// ; Delimita el final de una linea

console.log("linea");

// . Se utiliza en los objetos para acceder a los atributos

// cancha.anchura

// [] Se utiliza en lista y/o arrays

const array = [1, 2, 3, 4];
console.log(array[3]);

// () Se usa para funciones

function suma (a, b) {
    // Se escribe la funcion
}

// {} Llaves para funciones, objetos y estructuras de control

const movil = {
    ancho: 5,
    alto: 8
}