// Strings

let str_simp = 'Esto es un string con caracteres simples';
let str_dobl = 'Esto es un string con caracteres dobles';

console.log(str_simp);
console.log(str_dobl);

// Texto con comillas

let str_comillas = "Es solo un \"chiste\"";
let str_comillas_simples = 'Es solo un "chiste"';
let str_comillas_dobles = "Es solo un 'chiste'";

console.log(str_comillas);
console.log(str_comillas_simples);
console.log(str_comillas_dobles);

// Texto con comillas invertidas (backticks)

let nombre = "Juan Manuel";
let str_backticks = `Hola, ${nombre}`;

console.log(str_backticks)

// Plantilla HTML

let plantilla = `
    <html>
        <h1>Pagina web de ${nombre}</h1>
        <p>Esto es un parrafo</p>
    </html>
`

console.log(plantilla);